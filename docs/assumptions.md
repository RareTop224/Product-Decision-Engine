# Assumptions

- `recommended_monthly_volume` трактуется как верхняя граница опубликованного
  рекомендуемого диапазона. Если фактический месячный объём выше, модель
  исключается до ranking.
- Если рекомендуемый объём производителем не опубликован, отсутствие значения
  само по себе пока не исключает модель, но выводится как предупреждение рядом
  с рекомендацией.
- Ресурс стартового расходника сначала уменьшает спрос его собственного явно
  заданного канала. Ресурсы разных каналов не взаимозаменяемы.
- Покупаются целые упаковки replacement consumables.
- Устройство считается поставленным с одной установленной maintenance unit.
  Новая unit покупается только когда для продолжения печати превышен её ресурс.
- Headline TCO включает устройство, OEM consumables и опубликованные обязательные
  maintenance items. Бумага, электричество, ремонт, resale value, неоригинальные
  расходники и заправка исключены.
- Цена — ручное test observation на указанную дату, а не обещание актуальной
  рыночной цены.
- Headline TCO использует одно явно выбранное `primary`-наблюдение. Вторичные
  наблюдения не меняют детерминированный результат и используются только для
  консервативной проверки ценового диапазона.
- Стресс-тест цен независимо подставляет верхние наблюдаемые цены для текущего
  победителя и нижние для альтернативы. Это намеренно строгая граница, а не
  реалистичная корзина одного магазина: она может переоценивать вероятность
  переключения и не заменяет синхронный рыночный snapshot.
- Синхронная корзина одного продавца считается полной только при подтверждённом
  наличии и цене точного устройства, а также наличии, цене и URL всех OEM
  consumables, которые хотя бы один сравниваемый расчёт требует купить в
  заданном сценарии, на одну дату. Maintenance item или replacement с нулём
  покупок во всех сравниваемых расчётах на выбранном горизонте не обязан
  присутствовать в корзине. Статусы «ожидается», «в
  транзите», архивная/последняя цена и неподтверждённый срок выдачи не считаются
  наличием; неполная корзина не используется для TCO.
- Двумерный контроль доли цвета использует 750 страниц в месяц. Это не оценка
  типичного пользователя, а экспериментальная точка: на длинном горизонте она
  требует сменных расходников и при этом не превышает опубликованную для HP
  Smart Tank 580 рекомендуемую нагрузку 800 страниц в месяц.
- Если ресурс установленного maintenance item официально отличается от ресурса
  сменной детали, первая граница задаётся `installed_yield_value`, а последующие
  замены используют `Consumable.yield_value`.
- Для цветных устройств чёрный расходник относится ко всем страницам, а CMY —
  к цветным страницам. Фоновый расход CMY на калибровку и возможное использование
  цветного тонера при монохромной печати не моделируются без опубликованной
  детерминированной нормы; это может занижать TCO монохромно-нагруженных сценариев
  на цветном лазерном устройстве.
- Если официальный datasheet и руководство конкретной модели противоречат друг
  другу, выбранное значение должно иметь отдельное verified evidence, а второе
  значение сохраняется как `conflict` и выводится в отчёте.
- Для Canon PIXMA TS3640/TS7440a печатающая головка является частью сменного
  FINE-картриджа, поэтому отдельная плановая maintenance unit в TCO не нужна.
- У HP Smart Tank 580/720 заменяемые печатающие головки существуют, но в
  проверенных официальных спецификациях нет детерминированного планового ресурса.
  Статус хранится как `not_published`: стоимость не выдумывается, а риск явно
  остаётся в отчёте.
- В PostgreSQL `Product.status = active` пока означает только включение записи в
  исследуемый каталог. Это не доказательство текущего производства, продажи или
  наличия. В M2 допуск к recommendation set должен отдельно учитывать свежие
  availability observations и lifecycle status.
- Порог 30 дней в bootstrap-отчёте является диагностикой старых primary prices,
  а не production SLA. В первом срезе M2 утверждены проверяемые пилотные пороги:
  30 дней для обязательных цен и 7 дней для наличия. Они должны быть
  подтверждены или скорректированы по трём повторным срезам M3.
- Текущий Golden Dataset хранит даты наблюдений с точностью до дня. Переход к
  внутридневному автоматическому обновлению потребует отдельной миграции на
  timestamp, но не нужен для проверки bootstrap-паритета.
- Целевой рынок текущего пилота — Россия; первый регион наблюдения — Москва,
  цены хранятся в RUB. Данные и предложения СНГ не объединяются с этим срезом.
- Наличие карточки на Price.ru не считается доказательством текущей продажи.
  Нужны свежее предложение и явный статус наличия. Последняя историческая цена
  карточки с нулём продавцов не проходит freshness gate.
- Price.ru не является authoritative-источником technical facts. Он может быть
  price/availability provider только после exact SKU/OEM-фильтрации.
- Опубликованный KNS порог изменения курса ЦБ на 1% используется только как
  диагностический допуск при сопоставлении USD-прайса с RUB-карточками. Это не
  подтверждённая формула пересчёта: промо и другие правила ценообразования могут
  давать другую финальную цену. Verified-наблюдение должно содержать готовую
  RUB-цену из карточки или договорного feed.

## Открытые риски данных промежуточного набора

- Brother DCP-L2622DW пока исключён: официальный европейский datasheet и
  публичная карточка точного российского SKU расходятся по семейству сменных и
  ресурсу стартового картриджа. До подтверждения региональной комплектации
  модель нельзя безопасно включить в TCO.
- Цены HP 222X для HP Color LaserJet Pro MFP 3303fdw сохранены как ручные
  тестовые наблюдения, хотя карточки расходников на дату проверки отмечены как
  недоступные или архивные. Они пригодны для проверки математики, но не для
  утверждения о доступной сегодня покупке.
- Пять primary-наблюдений цен Pantum CTL-1100/CWT-1100 датированы 2025-12-28 и
  уже выявляются bootstrap-отчётом как старше 30 дней. Они не должны считаться
  актуальными после включения freshness gate.
- Среднее число страниц в одном задании пока не является частью сценария.
  Модели, для которых без него нельзя воспроизводимо рассчитать обязательные
  maintenance items, не добавляются в ranking.
